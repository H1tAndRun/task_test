package com.anton.spring_boot.task_test.service;

import com.anton.spring_boot.task_test.dto.CalculateDTO;
import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

class CalculateServiceTest {

    @Test
    void calculateVocationPay() {
       /* CalculateService calculateService = new CalculateService();
        String actual = calculateService
                .calculateVocationPay(new CalculateDTO(20000, "20-01-2011", "25-01-2011"));
        String expected = "Сумма отпускных составляет 4095,56";
        assertEquals(expected, actual);*/
    }
}